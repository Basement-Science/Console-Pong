namespace ConsolePong {
    internal class Ball {
        private object x;
        private object y;

        public Ball(object x, object y) {
            this.x = x;
            this.y = y;
        }
    }
}